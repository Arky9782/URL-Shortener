<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Welcome</title>
            <link rel="stylesheet" href="{{ asset('css/link.css') }}">
            <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    </head>

    <body>
        <div class="form">

             {{ form_start(form) }}
             {{ form_widget(form) }}
             {{ form_end(form) }}

        </div>

    {% if userLink is not null %}
    <div class="page-link">
        <h1 class="nav-link">Done! Your link: {{ host }}/{{ userLink }}</h1>
    </div>
    {% endif %}

    </body>
</html>
